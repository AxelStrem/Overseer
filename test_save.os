tab main (title="Save Test") {
    text header = "Testing Save Functionality"
    
    string name = "Test User"
    int age = 25
    checkbox complete(label="Task Complete") = false
    checkbox verified(label="Verified") = true
}
